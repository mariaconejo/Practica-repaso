const text = prompt('Enter your text').split(''); // split convierte de string a un arreglo

console.log(text);
text.reverse(); // convierte la palabra al reves
console.log(text.join(' ')); // convierte de un arreglo a un string